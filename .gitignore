.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
.quartz/
private/
.replit
replit.nix

# graph plugin fork lives in its own repo (github:gborghi/graph); kept on disk for
# editing but not tracked here — referenced via quartz.lock.json
plugins/

# local dev artifacts
test-server*.mjs
.playwright-mcp/
verify-page.*
